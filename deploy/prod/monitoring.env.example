# Monitoring env.
#
# Copy to `deploy/prod/monitoring.env` and set strong values.
# It is intentionally separate from `prod.env`.
# Also configure deploy/prod/alertmanager.yml with your real on-call receiver.
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=
# Pin monitoring image versions (change intentionally during maintenance windows).
PROMETHEUS_VERSION=v2.50.1
ALERTMANAGER_VERSION=v0.27.0
BLACKBOX_EXPORTER_VERSION=v0.25.0
GRAFANA_VERSION=10.4.5

# Docker json-file log rotation for monitoring stack.
DOCKER_LOG_MAX_SIZE=20m
DOCKER_LOG_MAX_FILE=5
